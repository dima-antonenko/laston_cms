<h2>This is  product page</h2>
<% @product.attributes.each do |attribute| %>
<%= attribute %><br/>
<% end %>
<hr/>
<%= button_to 'Купить', line_items_path(product_id: @product), { id: "button-cart", class: "button"} %>
<hr/>

Купить в один клик:
<%= form_tag('/orders/create_quick') do %>

  <%= text_field_tag 'customer_phone', 'Номер телефона'  %>
  <%= hidden_field_tag 'product_id', @product.id %>
  <%= submit_tag 'Save' %>

<% end %>

<hr/>
Задать вопрос по товару:
<%= form_for @product_question do |f| %>
  <%= f.text_field :name, placeholder: "Имя" %><br/>
  <%= f.text_field :phone, placeholder: "Номер телефона" %><br/>
  <%= f.text_area :text, placeholder: "Текст вопроса" %><br/>

  <%= f.hidden_field :product_id, value: @product.id %>
  <%= f.submit "Отправить" %>
<% end %>


<hr/>
Заказать обратный звонок:
<%= form_for @back_call do |f| %>
  <%= f.text_field :phone, placeholder: "Номер телефона" %>
  <%= f.hidden_field :product_id, value: @product.id %>
  <%= f.submit "Отправить" %>
<% end %>

<hr/>
Отправить заявку form_request:
<%= form_for @form_request do |f| %>
  <%= f.text_field :name, placeholder: "Имя" %><br/>
  <%= f.text_field :email, placeholder: "Email" %><br/>
  <%= f.text_field :phone, placeholder: "Номер телефона" %><br/>
  <%= f.text_area :text, placeholder: "Текст вопроса" %><br/>

  <%= f.hidden_field :product_id, value: @product.id %>
  <%= f.submit "Отправить" %>
<% end %>
